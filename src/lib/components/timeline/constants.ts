export const TICK_PADDING = 8; // padding for left positions so the ticks doesn't stick to the edge
export const TARGET_TICK_WIDTH = 200; // desired tick width in pixels
export const TICK_INTERVALS = [0.1, 0.25, 0.5, 1, 2, 5, 10, 30, 60, 300]; // in seconds
